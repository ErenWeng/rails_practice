<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>

<body>
  <h1>使用者註冊</h1>

  <%= form_for(@user, url: '/sign_up', method: 'post') do |f| %>
  <% if @user.errors.any? %>
  <ul>
    <% @user.errors.full_messages.each do |messages| %>
    <li><%= messages %></li>
    <% end %>
  </ul>
  <% end %>


  <div>
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>

  <div>
    <%= f.label :password %>
    <%= f.password_field :password %>
  </div>

  <div>
    <%= f.label :password_confirmmation, "確認密碼"%>
    <%= f.password_field :password_confirmation %>
  </div>

  <div>
    <%= f.submit "註冊"%>
  </div>

  <% end %>

</body>

</html>